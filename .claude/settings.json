{
  "attribution": {
    "commit": "",
    "pr": ""
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q '\"git commit'; then echo '[V-07] commit detectado — cache de versão será renovado no próximo request'; fi"
          }
        ]
      }
    ]
  }
}
